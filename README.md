# Binance Futures Testnet Trading Bot

##  Setup
1. Register on Binance Futures Testnet.
2. Generate API keys and paste them in `cli.py`.
3. Install dependencies:
   ```bash
   pip install -r requirements.txt
